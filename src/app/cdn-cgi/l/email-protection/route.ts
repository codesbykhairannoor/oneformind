import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.redirect('https://tranvas.com', 301);
}

export async function POST() {
  return NextResponse.redirect('https://tranvas.com', 301);
}
